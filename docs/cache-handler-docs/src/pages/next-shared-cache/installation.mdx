## Installation

To install the `@neshca/cache-handler` package, run the following command:

```sh npm2yarn copy
npm i -D @neshca/cache-handler
```

## Usage with Redis

Redis is a peer dependency of this package, so you need to install it separately:

```sh npm2yarn copy
npm i -D redis
```
