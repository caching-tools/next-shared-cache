{
    "$schema": "https://turbo.build/schema.json",
    "globalDependencies": ["**/.env.*local"],
    "globalEnv": ["CI"],
    "pipeline": {
        "build": {
            "dependsOn": ["^build"],
            "env": ["REDIS_URL"],
            "outputs": [".next/**", "!.next/cache/**", "dist/**"]
        },
        "lint": {},
        "clean": {
            "cache": false
        },
        "start": {
            "cache": false,
            "persistent": true
        },
        "dev": {
            "cache": false,
            "persistent": true
        }
    }
}
